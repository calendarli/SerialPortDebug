# 项目协作规则

## 修改完成后同步更新版本号

- 每完成一批交付给用户的项目修改，都必须同步更新应用版本号，包括功能、缺陷修复、界面、配置和文档修改。纯审查、问答或没有文件变更的操作不递增版本。
- 用户未指定版本时，默认将补丁版本号加一，例如 `3.2.5` → `3.2.6`；用户明确指定版本时，以用户要求为准。
- 同一批修改只递增一次，不因反复编辑、验证、构建或随后执行 Git 提交再次递增。用户提出新的修改并完成交付时，再递增一次。
- 必须同步更新以下三个位置，保持完全一致：
  - `package.json` 的 `version`。
  - `package-lock.json` 顶层的 `version`。
  - `package-lock.json` 中 `packages[""].version`。
- 应用界面继续通过 `app.getVersion()` 获取版本；安装包文件名继续使用构建配置中的 `${version}`，不要另写硬编码版本号，也不要修改依赖包或协议的版本号。
- 可以使用 `npm version patch --no-git-tag-version --ignore-scripts` 更新版本，或仅修改上述三个字段。不要为了递增版本自动创建 Git 提交、标签、推送或发布。
- 交付或提交前检查三个版本字段一致，并运行与修改相符的检查。最终回复中说明本次的新版本号。
